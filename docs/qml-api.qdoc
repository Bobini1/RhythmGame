/*!
    \qmlmodule RhythmGameQml
    \title RhythmGame QML Types
    \brief Reusable behavior and data components available to every skin.

    The module provides optional building blocks rather than a required skin
    framework. A skin can use a complete controller such as
    \l StandardSelectController, select only lower-level components, or replace
    individual actions while retaining the standard behavior for everything
    else.
*/

/*!
    \page qml-api.html
    \title QML API Reference

    These components contain reusable behavior and state. Skins remain
    responsible for presentation and may instantiate only the pieces they need.

    \section1 Choosing a component

    \table
        \header
            \li Goal
            \li Start with
            \li The skin still owns
        \row
            \li A complete song selector
            \li \l StandardSelectController
            \li The list or wheel, focus animation, and Internet-ranking UI
        \row
            \li Custom browsing with standard input
            \li \l StandardSelectState with \l StandardSelectInput and, if
                wanted, \l StandardSelectShortcuts
            \li Presentation and the chosen composition
        \row
            \li Custom input with standard browsing
            \li \l StandardSelectState
            \li Input mapping and presentation
        \row
            \li Only folder resolution or activation
            \li \l StandardSelectSession or \l StandardSelectActivation
            \li History transactions, focus, filtering, input, and presentation
    \endtable

    Callback properties use these contracts:

    \list
        \li A property documented as a replacement suppresses the matching
            built-in operation when it contains a function. Its return value is
            ignored.
        \li A property named \c try...Action is a pre-handler. Return \c true to
            consume the operation; return \c false or \c undefined to continue
            with the built-in operation.
        \li Supplemental callbacks, such as
            \l StandardGameplayExit::closePresentationAction, are explicitly
            documented and do not replace the surrounding operation.
        \li Request signals identify operations that require skin-owned UI or
            could not be completed by the standard implementation.
    \endlist

    \section1 Selection

    \section2 Complete standard selector

    \list
        \li \l StandardSelectController extends the standard selection state with
            presentation adaptation, feedback, input, navigation, and shortcuts.
    \endlist

    \section2 Browsing and activation

    \list
        \li \l StandardSelectState owns browsing, filtering, sorting, scores,
            focus, and activation.
        \li \l StandardSelectSession provides lower-level folder contents,
            history, and score-query lifetime.
        \li \l StandardSelectActivation provides lower-level chart and course
            activation.
        \li \l StandardSelectModelAdapter repeats logical entries for circular
            presentation.
        \li \l PendingReplyGroup owns cancellable asynchronous work with a shared lifetime.
    \endlist

    \section2 Interaction policies

    \list
        \li \l StandardSelectInput extends navigation with keyboard and
            controller action mappings.
        \li \l StandardSelectNavigation implements directional and scratch
            navigation.
        \li \l StandardSelectShortcuts implements selection-specific F-key
            shortcuts.
    \endlist

    \section1 Other screens and application behavior

    \list
        \li \l StandardMainActions provides main-menu destinations.
        \li \l StandardDecideFlow handles decide-screen lifetime and input.
        \li \l StandardResultInput handles result dismissal and retry input.
        \li \l StandardGameplayExit handles gameplay escape behavior.
        \li \l StandardGameplayAttemptState tracks whether a play was attempted.
        \li \l StandardInputKeys classifies standard gameplay input keys.
    \endlist
*/
