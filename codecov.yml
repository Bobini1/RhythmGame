coverage:
  status:
    project:
      default:
        target: 75%    # the required coverage value
        threshold: 1%  # the leniency in hitting the target
        informational: true
    patch:
      default:
        target: 75%    # the required coverage value
        threshold: 1%  # the leniency in hitting the target
        informational: true
ignore:
  - "test"
